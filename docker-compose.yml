version: '2.2'
services:
    redis:
        image: registry.lil.tools/library/redis:6.0.11
    web:
        image: registry.lil.tools/harvardlil/jcushman/actions-test/web:0.1-badhash
        volumes:
            - .:/app
        depends_on:
            - redis
        tty: true
