version: '2'
services:
    redis:
        image: registry.lil.tools/library/redis:6.0.11
    web:
        build: .
        image: ghcr.io/jcushman/actions-test:0.1
        volumes:
            - .:/app
        depends_on:
            - redis
        tty: true
